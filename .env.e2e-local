# Environment Variables for Local E2E Testing
# This file configures the isolated test environment
# Copy this to .env.e2e-local.local for local overrides (gitignored)

# ============================================================
# DATABASE CONFIGURATION
# ============================================================
DB_HOST=postgres-test
DB_PORT=5432
DB_NAME=st44_test_local
DB_USER=postgres
DB_PASSWORD=postgres

# ============================================================
# BACKEND CONFIGURATION
# ============================================================
PORT=3000
HOST=0.0.0.0
NODE_ENV=test

# CORS Configuration (allow all for local testing)
CORS_ORIGIN=*

# JWT Secrets (test environment only - not for production!)
JWT_SECRET=test-secret-key-local-e2e
JWT_REFRESH_SECRET=test-refresh-secret-key-local-e2e

# ============================================================
# FRONTEND CONFIGURATION
# ============================================================
CI=false

# ============================================================
# PORT MAPPINGS (Host:Container)
# ============================================================
# PostgreSQL:  5433:5432 (avoids conflict with dev DB on 5432)
# Backend:     3001:3000 (avoids conflict with dev backend on 3000)
# Frontend:    4201:4200 (avoids conflict with dev frontend on 4200)

# ============================================================
# NOTES
# ============================================================
# - All services communicate via Docker network internally
# - Host machine accesses services via mapped ports above
# - Database initializes with schema from docker/postgres/init.sql
# - Frontend proxy is configured to route /api to backend:3000
# - Use 'localhost:3001' to access backend from host
# - Use 'localhost:4201' to access frontend from host
# - Use 'localhost:5433' to access database from host
